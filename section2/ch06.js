/*
 순회 : 배열, 객체에 저장된 여러 개의 값에 순서대로 하나씩 접근하는 것

 순회하는 방법 : for, for of 반복문

 객체 순회
 1.Objects.keys
 객체에서 key 값들만 뽑아서 새로운 배열로 반환

 2.Object.values
 객체의 value값들마나 뽑아서 새로운 배열로 반환
 
 3.for in
 for(let key in 객체){
    console.log(key) 객체의 key를 key에 할당해준다.
 }
 */

// let arr2 = [1, 2, 3, 4, 5];
// console.log(
//   arr2.map((item) => {
//     console.log(item);
//   })
// );
